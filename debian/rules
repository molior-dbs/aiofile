#!/usr/bin/make -f

export PYBUILD_NAME = aiofile

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_build:
	cython3 aiofile/posix_aio.pyx
	dh_auto_build

override_dh_auto_clean:
	rm -f aiofile/posix_aio.c
	dh_auto_clean

override_dh_auto_test:
	pytest-3

